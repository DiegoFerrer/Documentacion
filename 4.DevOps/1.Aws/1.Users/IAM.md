<!-- 
* IAM (Identidad y acceso management)
    # Identidad | Acceso tanto para individuos como una app
    # Agrupa    | Usuarios en comun, administracion de multiples usuarios
    # Role      | Para servicios AWS, para comunicarse entre servicios

    ? Politicas
    Podemos permitir o denegar acceso a los servicios. a los grupos,indivuos o roles
        * Inline policy | alcance al nivel de identidad
        * Custom Managed | alcance a nivel mas global para ser reutilizadas por varias entidades
        * Job Function (politicas ya creadas por AWS que podemos usar)

    
    todo, Modificar URL IAM
    todo, Establecer politicas de contraseñas


    ? Crear usuario
        * Access Type
            # Programmatic access | Para tener una llave privada y usarlo desde terminales
            # AWS Management console | para usarlo desde la consola WEB de AWS
        * Policies | Permisos a la hora de crear los IAM
            # Seleccionar el usuario a editar desde root y poner crear policy 
            # Darle nivel de acceso a los diferentes servicios










* CloudWatch 
* SNS (simple notifications services) 

-->