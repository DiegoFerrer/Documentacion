<!-- 
? Servicio VPC (Virtual Private Cloud)
    Cuando creas tu cuenta en aws te llega la VPC por default, aunque podes crear una nueva
    # Aislamos una porcion logica de los datacenter de amazon (de la region).
    # La subnet es a nivel de (zona de disponibilidad)

    Para que una VPC se comunique con internet, se necesita un internet gateway, que permite esta comunicacion.

    

-->