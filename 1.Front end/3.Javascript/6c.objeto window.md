
<!--          OBJETO WINDOW                       -->
<script>
	// Dimensiones
	.innerWidth			// ancho contenido
	.innerHeigth		// alto contenido
	.outerWidth			// ancho de la ventana entero
	.outerHeight		// alto de la ventana entero
	.screenX			// devuelve donde esta la ventana en horizontal
	.screenY			// devuelve donde esta la ventana en vertical

	// Scroll
	.scrollX			// scroll en horizontal
	.scrollY			// scroll en vertical

	// Dialogos del objeto window
	.status				// sentencia de status
	.alert()			// ventana de alerta
	.confirm()			// ventana de confirmacion
	.prompt()			// ventana de input
	
	// Manipular ventanas
	.open('url')
	.close()
	.getSelection()
	.getComputeStyle()		// devuelve el objeto de los estilos css computados
	.scrollTo(x,y)			// nos envia a la posicion x,y
	.scrollBy(x,y)			// suma a la posicion actual,los valores x ,y


</script>


