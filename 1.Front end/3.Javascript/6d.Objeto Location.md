<script>
	

// OBJETO LOCATION

// NOS ENTREGA LA INFORMACION DE LA URL

location.href 		// obtiene la url actual
location.host 		// obtiene el dominio y el puerto
location.hostname 	// Obtiene dominio
location.protocol	// Obtiene el protocolo | https ejemplo
location.origin		// Obtiene el protocolo + el dominio
location.hash		// Si hay algun #hash
location.pathname	// La ruta interna

// Metodos

location.reaload() 		// Recarga la pagina 
location.assign('url')	// Cambia la navegacion
location.replace('url') // Cambia la navegacion


</script>
