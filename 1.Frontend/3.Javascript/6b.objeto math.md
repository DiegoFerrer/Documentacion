
<!--              objeto Math                   -->


<script>
    e 		// constante de eurir | numero e
    LN2		// log de 2
    LN10	// log de 10
    LOG2E 	// log de e en base 2
    LOG10E 	// log de e en base 10
    PI		// Numero PI
    SQRT1_2 // Razidcuadrada de 0.5
    SRQRT2	// Raiz cuadrada de 2

    //	 Metodos
    abs(n)		// calcula el valor absoluto de n
    acos(n)		// calcula el arco coseno de n
    asin(n)		// calcula el arco seno de n
    atan(n)		// calcula el arco tangente de n
    ceil(n)		// redondea un numero hacia arriba
    cos(n)		// calcula el coseno de n
    exp(n)		// calcula el exponencial de e
    floor(n)	// redondea hacia abajo
    log(n)		// calcula el log de n
    round(n)	// redondea el n depende el decimal hacia arriba o abajo
    random(n)   // redondea aleatorio
    sin(n)		// calcula el seno de un n
    sqrt(n)		// calcula la raiz cuadrada de n
    tan(n)		// calcula la tangente de n
    max(x,y)	// x / y en funcion de cual es mayor
    min(x.y)	// x / y en funcion de cual es menor

    // En ES6 no habia forma de pasarle arrays a los metodos pero en ES6
    // Con el spret operator si math.metodo(...array)

</script>