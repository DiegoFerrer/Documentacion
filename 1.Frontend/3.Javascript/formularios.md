
<!--           Formulaios                   -->

<script>
	/* 
		•  RADIO BUTTONS 

		// Cuando se dispone de un grupo de radiobuttons, generalmente no se quiere obtener el valor del atributo value de alguno de ellos, si no que lo importante es conocer cual de todos los radiobuttons se ha seleccionado.

		La propiedad checked devuelve true para el radiobuttons seleccionado
	*/

	// radio seleccionado
	let elementos = document.getElementsByName('nameAtributo')
	const RadioSelect = radios => {
		radios.filter(radio => {
			radio.checked
		})
	}
	}

	// CHECKBOX 

	// Pasa lo mismo que los radio pero se pueden seleccionar multiples

	// METODO RESET

	form.reset() // luego de hacer un submit, se pone este metodo para reinciar el formulario
</script>