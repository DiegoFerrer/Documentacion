
/*
	# SCAFFOLDING: Generar la estructura de proyecto
	# PREVIEW: Cambios reflejados automaticamente
	# LOCAL BUILD: el deploy listo para distribuir
	# TESTING: Unit test
	# BUENAS PRACTICAS: 
------------------------------------------------------------------------------------------------
?			DOCUMENTACION
https://angular.io/cli

------------------------------------------------------------------------------------------------

	******************************* INSTALAR CLI *******************************	 
  	#1. Linea de comandos (Node,etc)
   		a. npm install -g @angular/cli 
   		b. ng --version (para ver la version)

	******************************* CREAR PROYECTO ANGULAR *******************************	 
    #2. ng new name-proyecto 
		--routing 		| Crea un modulo para el manejo de rutas
		--prefix name 	| Un prefijo para crear directivas y SELECTOR componente
		--style scss	| Que tipo de extension van a ser los estilos
		--skip-install 	| Para tener el control de las dependencias
		--dry-run		| Te mostraria que archivos crearia el proyecto (no lo crea)
		npm install --save
		ng lint| verifica que se sigan las reglas del archivo tslint, para que no haya errores


////#############################################################################################################
?										MANEJAR EL CLI
* ng g --help 		|  Para ver que te puede generar
* ng g c --help 	|  Te muestra todas las banderas y que hace cada una, disponibles para el componente
# ng generate component name
# ng generate module name
# ng generate service name
# ng generate interfaces name
----------------------------
--skipTests=true corresponde a la exclusión de los ficheros de los tests que no vamos a necesitar en este caso.
		 
////#############################################################################################################
?										A
 
*/

